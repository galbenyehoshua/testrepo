Import-Csv "C:\path\to\users.csv" | ForEach-Object { if ((Get-ADGroupMember -Identity "SecurityGroupName" | Where-Object { $_.SamAccountName -eq $_.Username })) { Write-Host "$($_.Username) is already a member of the group." } else { Write-Host "$($_.Username) is NOT a member of the group." } }
